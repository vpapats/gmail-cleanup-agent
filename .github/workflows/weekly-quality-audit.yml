name: Gmail Weekly Quality Audit

on:
  schedule:
    # The gate below selects the one entry that is 09:00 Europe/Athens across DST.
    - cron: "0 6 * * 1"
    - cron: "0 7 * * 1"
  workflow_dispatch:
    inputs:
      force:
        description: "Force a corrective rerun even if this Review ID was already emailed"
        required: false
        default: false
        type: boolean

permissions:
  actions: read
  contents: write

concurrency:
  group: gmail-weekly-quality-audit
  cancel-in-progress: false

jobs:
  weekly-audit:
    if: github.event_name != 'schedule' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
      GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY_AETHERISPC }}
      OPENROUTER_MODEL: google/gemini-3.1-flash-lite
      OPENROUTER_MAX_ATTACHMENT_BYTES: ${{ vars.OPENROUTER_MAX_ATTACHMENT_BYTES || '750000' }}
      GMAIL_FOMO_STATE_KEY: ${{ secrets.GMAIL_FOMO_STATE_KEY }}
      WEEKLY_REVIEW_APPROVAL_SECRET: ${{ secrets.WEEKLY_REVIEW_APPROVAL_SECRET }}
      WEEKLY_REVIEW_APP_URL: ${{ vars.WEEKLY_REVIEW_APP_URL }}
      GMAIL_FOMO_FORCE_WEEKLY_AUDIT: ${{ inputs.force || false }}
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - name: Select 09:00 Europe/Athens run
        id: schedule-gate
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || [ "$(TZ=Europe/Athens date +%H)" = "09" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        if: steps.schedule-gate.outputs.run == 'true'
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}

      - name: Set up Python
        if: steps.schedule-gate.outputs.run == 'true'
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.schedule-gate.outputs.run == 'true'
        run: python -m pip install -r requirements.txt

      - name: Validate required credentials
        if: steps.schedule-gate.outputs.run == 'true'
        run: |
          missing=0
          for key in GOOGLE_CLIENT_ID GOOGLE_CLIENT_SECRET GOOGLE_REFRESH_TOKEN OPENROUTER_API_KEY GMAIL_FOMO_STATE_KEY WEEKLY_REVIEW_APPROVAL_SECRET WEEKLY_REVIEW_APP_URL GITHUB_TOKEN; do
            if [ -z "${!key}" ]; then
              echo "::error::Missing required credential: ${key}"
              missing=1
            fi
          done
          exit "$missing"

      - name: Run weekly quality auditor
        if: steps.schedule-gate.outputs.run == 'true'
        run: PYTHONPATH=. python scripts/run_weekly_audit.py
